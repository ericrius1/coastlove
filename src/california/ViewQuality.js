// Distance is independent of the picture-resolution setting. Coastlove keeps
// the original close geometry and extends lightweight silhouettes map-wide.
export const COAST_VIEW = Object.freeze({
 renderScale: 1, shadowSize: 2048, hazeDensity: .7,
 terrainRange: 3, treeFade: [10000,12000], landmarkRange: 12000,
 maxTrees: 6500, maxShrubs: 14000,
});
